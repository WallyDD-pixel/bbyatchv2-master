# Configuration de la base de données PostgreSQL
# Pour Docker Compose (port 5433) :
DATABASE_URL="postgresql://bbyatch:change_me_strong@localhost:5433/bbyatch_preprod?schema=public"

# Pour PostgreSQL installé directement (port 5432) :
# DATABASE_URL="postgresql://user:password@localhost:5432/bbyatch_preprod?schema=public"

# URL de l'application (utilisée par NextAuth)
NEXTAUTH_URL="https://preprod.bbservicescharter.com"

# Secret pour NextAuth (générez-en un avec: openssl rand -base64 32)
NEXTAUTH_SECRET="votre-secret-tres-long-et-aleatoire-ici"

# Clés Stripe (si vous utilisez Stripe pour les paiements)
STRIPE_TEST_PK="pk_test_..."
STRIPE_TEST_SK="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Port sur lequel l'application Next.js va tourner
PORT=3010

# Optionnel: Pour forcer SWC Rust WASM (Windows seulement)
# NEXT_FORCE_SWCRUST_WASM=1

